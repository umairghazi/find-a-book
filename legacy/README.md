# Legacy app

This folder preserves the original 2015 "Found You a Book" project before the React Router v7 relaunch.

The old version used global JavaScript variables, chained select menus, manual DOM updates, Bootstrap, browser detection, and a Netlify Lambda proxy for the Goodreads XML API. It is kept here for historical comparison and portfolio storytelling.
